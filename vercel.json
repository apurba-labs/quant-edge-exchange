{
    "buildCommand": "turbo run build --filter=web",
    "outputDirectory": "apps/web/.next",
    "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ]
}